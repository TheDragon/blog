		<h1><%= @posts.title %></h1>
		<ul><%= simple_format @posts.body %></ul>
		<br />
		<%= link_to 'Back to list', { :action => 'index' } %>
		<%= render :partial => 'home/add_comment', :locals => { :post => @posts } %>
